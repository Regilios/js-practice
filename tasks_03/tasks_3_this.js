/** 
 *  this — это не то, где функция была написана, а как она была вызвана. 
    Ключевое правило:
    this определяется в момент вызова функции, а не при её объявлении.  

    💡  this — это не ссылка функции на саму себя и это не ссылка на область видимости функции.
    В действительности this — это привязка, которая создается во время вызова функции, и на что она ссылается определяется тем, где и при каких условиях функция была вызвана.
*/


function f1() {
  return this;
}
// В браузере:
f1() === window; // window - глобальный объект в браузере
// В Node:
f1() === global; // global - глобальный объект в Node

function f2() {
  "use strict"; // см. strict mode
  return this;
}
f2() === undefined; // true






function identify() {
	return this.name.toUpperCase();
}

function speak() {
	var greeting = "Hello, I'm " + identify.call( this );
	console.log( greeting );
}

var me = {
	name: "Kyle"
};

var you = {
	name: "Reader"
};

identify.call( me ); // KYLE
identify.call( you ); // READER

speak.call( me ); // Hello, I'm KYLE
speak.call( you ); // Hello, I'm READER

/*  💡 В строгом режиме ("use strict") в таком коде значением this будет являться undefined. Если мы попытаемся получить доступ к this.name – это вызовет ошибку.

    В нестрогом режиме значением this в таком случае будет глобальный объект (window в браузерe).
    Это – исторически сложившееся поведение this, которое исправляется использованием строгого режима ("use strict").

    Обычно подобный вызов является ошибкой программирования. Если внутри функции используется this, тогда она ожидает, что будет вызвана в контексте какого-либо объекта.

    Значение this определяется во время исполнения кода.

    При объявлении любой функции в ней можно использовать this, но этот this не имеет значения до тех пор, пока функция не будет вызвана.
    Функция может быть скопирована между объектами (из одного объекта в другой).
    Когда функция вызывается синтаксисом «метода» – object.method(), значением this во время вызова является object.
*/




function makeUser() {
  return {
    name: "John",
    ref: this  // ← здесь this — это this из makeUser()
  };
}

let user = makeUser();
/**
 *  В момент выполнения этой строки:
    Объект ещё не создан — мы его только собираемся вернуть.
    this вычисляется сейчас, в контексте выполнения функции makeUser.
    Этот this не имеет никакого отношения к будущему объекту { name: "John", ref: this }.
    ❌ Нет, JavaScript не делает магии и не подставляет вместо this тот объект, который будет создан.
    ✅ this — это всегда результат правила вызова функции. 
    
    💡 Объект { name: "John", ref: this } создаётся внутри функции, но он ещё не присвоен никакой переменной.
    Никакой объект не вызывает эту функцию.
    this не может «предвидеть», что в будущем кто-то присвоит результат функции переменной и назовёт его user.
    JavaScript не смотрит вперёд и не думает:

    «О, сейчас создастся объект, и его, наверное, хотят привязать к this» — такого нет. 

     Чтобы сослаться на сам объект, нужно: 

    либо использовать метод (где this определится при вызове),
    либо явно присвоить ссылку на объект после его создания.
 */
alert( user.ref.name ); // Error: Cannot read property 'name' of undefined 


{
    function makeUser() {
      return {
          name: "John",
          ref() {
            return this;
          }
      };
    }

    let user = makeUser();
    alert( user.ref().name ); // John 
    /**
     * Здесь ref — это метод объекта, и он вызывается как метод:
     * Когда метод вызывается на объекте (obj.method()), this внутри метода автоматически становится тем объектом, к которому он привязан — в данном случае user.
     * ref() {return this; } — это функция, и this в ней определяется в момент вызова, а не при создании объекта. А при вызове user.ref() — this === user.
     */
}




var obj = { a: "Custom" };
// Это свойство принадлежит глобальному объекту
var a = "Global";

function whatsThis() {
  return this.a; //значение this зависит от контекста вызова функции
}

whatsThis(); // 'Global'
whatsThis.call(obj); // 'Custom'
whatsThis.apply(obj); // 'Custom'
// Для того, чтобы при вызове функции установить this в определённое значение, используйте call() или apply(), как в следующих примерах. 
// В качестве первого аргумента методов call или apply может быть передан объект,
// на который будет указывать this.





function bar() {
  console.log(Object.prototype.toString.call(this));
}

bar.call(7); // [object Number]
bar.call("foo"); // [object String]
/**
 * Обратите внимание, что в нестрогом режиме, если значение, переданное в call или apply как this, 
 * не является объектом, будет сделана попытка преобразовать его в объект с помощью внутренней операции ToObject. 
 * Таким образом, если переданное значение является примитивом, таким как 7 или 'foo', 
 * оно будет преобразовано в Object с использованием связанного конструктора, так что примитивное число 7 будет преобразовано в объект так, как будто с помощью new Number(7), 
 * а строка 'foo' - как будто с помощью new String('foo'), например
 */


var o = {
  f: function () {
    return this.a + this.b;
  },
};
var p = Object.create(o);
p.a = 1;
p.b = 4;

console.log(p.f()); // 5
/**
 * В этом примере объект, который присвоен переменной p, не имеет собственного свойства f, а наследует это свойство от своего прототипа.
 * Однако, совершенно неважно, что поиск свойства f в конце концов обнаружит его на объекте o. 
 * Поскольку поиск начался с p.f, то и свойство this внутри функции f будет ссылаться на объект p. 
 * Таким образом, если f вызывается как метод p, то и this относится к p. Это полезная особенность прототипного наследования JS.
 */



function C() {
  this.a = 37;
}

var o = new C();
console.log(o.a); // logs 37

function C2() {
  this.a = 37;
  return { a: 38 };
}

o = new C2();
console.log(o.a); // logs 38

/**
 * В примере (C2), из-за того, что конструктор вернул объект, новый объект, к которому было привязано this, был просто отброшен. 
 * (Это фактически делает выражение "this.a = 37;" "мёртвым" кодом. 
 * Он не является буквально нерабочим, так как он выполняется, но он может быть изъят без каких-либо внешних эффектов.)
 */






/**
 * call — вызов функции с указанием this и аргументами
 * func.call(thisArg, arg1, arg2, ...)
 */
function introduce(age, city) {
  console.log(`Меня зовут ${this.name}, мне ${age}, живу в ${city}`);
}

const person = { name: "Иван" };
introduce.call(person, 25, "Москва");
// → "Меня зовут Иван, мне 25, живу в Москва"




/**
 * apply — как call, но аргументы передаются массивом
 */
{
function introduce(age, city) {
  console.log(`Меня зовут ${this.name}, мне ${age}, живу в ${city}`);
}

const person = { name: "Мария" };
const args = [30, "СПб"];
introduce.apply(person, args);
// → "Меня зовут Мария, мне 30, живу в СПб"
}




/**
 * bind — создаёт новую функцию с привязанным this
 * const boundFunc = func.bind(thisArg, arg1, arg2, ...)
 */
{
function introduce(age, city) {
  console.log(`Меня зовут ${this.name}, мне ${age}, живу в ${city}`);
}

const person = { name: "Олег" };
// Создаём новую функцию, где this всегда = person
const introduceOleg = introduce.bind(person);

introduceOleg(40, "Казань");
// → "Меня зовут Олег, мне 40, живу в Казань"
}




  class Button {
    constructor(name) {
      this.name = name;
    }

    click() {
      console.log(`${this.name} нажата`);
    }
  }
  // Без bind: this потерян!
  // document.addEventListener('click', button.click); // this = window
  // С bind: this сохранён
  const button = new Button("Кнопка");
  document.addEventListener('click', button.click.bind(button));
/**
 * this определяется в момент вызова, а не в момент определения   В JavaScript значение this зависит от того, КАК вызвана функция, а не от того, где она была создана.
 * document.addEventListener('click', button.click); Ты передаешь ссылку на функцию, а не вызываешь её. Браузер позже вызовет эту функцию сам, примерно так: callback();
 * 
 * document.addEventListener('click', button.click);
   Здесь button.click передаётся как колбэк, и когда событие срабатывает, функция вызывается не в контексте объекта button, а в контексте элемента DOM (document).

  👉 Проблема: this внутри click() теперь указывает не на button, а на document (или другой элемент, который вызвал событие).
 */



/**
 * Вызывая foo(..) с new впереди нее, мы конструируем новый объект и устанавливаем этот новый объект как this для вызова foo(..).
 * Таким образом new — единственный путь, которым this при вызове функции может быть привязан. Мы называем это привязкой new.
 * callback(); // где callback = button.click
 * 
 *  1. Вызов как метода — this работает  button.click(); // → "Кнопка"
 *  2. Передача по ссылке — this теряется 
 *    const func = button.click; 
 *    func();  → undefined (в strict mode) или window.name (в нестрогом)
 */
function foo(a) {
	this.a = a;
}

var bar = new foo( 2 );
console.log( bar.a ); // 2






function foo(a,b) {
	console.log( "a:" + a + ", b:" + b );
}

// наш пустой DMZ-объект
var ø = Object.create( null );

// распаковываем массив как параметры
foo.apply( ø, [2, 3] ); // a:2, b:3

// каррируем с помощью `bind(..)`
var bar = foo.bind( ø, 2 );
bar( 3 ); // a:2, b:3

/**
 * Пожалуй в некоторой степени "более безопасная" практика — передавать особым образом настроенный объект для this, 
 * который гарантирует отсутствие побочных эффектов в вашей программе. Заимствуя терминологию из сетевых (и военных) технологий, 
 * мы можем создать объект "DMZ" (демилитаризованной зоны (de-militarized zone)) — не более чем полностью пустой, неделегированный (см. главы 5 и 6) объект.

  Если всегда передавать DMZ-объект для привязок this, которые не требуются, то мы можем быть уверены в том,
  что любое скрытое/неожидаемое использование this будет ограничено пустым объектом, который защитит объект global нашей программы от побочных эффектов.

  Поскольку этот объект совершенно пустой, лично я люблю давать его переменной имя ø (математический символ
  пустого множества в нижнем регистре). На многих клавиатурах (как например US-раскладка на Mac), 
  этот символ легко можно ввести с помощью ⌥+o (option+o). В некоторых системах есть возможность назначать горячие клавиши на определенные символы.
  Если вам не нравится символ ø или на вашей клавиатуре сложно набрать такой символ, вы конечно же можете назвать переменную как вам угодно.

  Как бы вы ни назвали ее, самый простой путь получить абсолютно пустой объект — это Object.create(null) (см. главу 5).
  Object.create(null) — похож на { }, но без передачи Object.prototype, поэтому он "более пустой", чем просто { }.
 * 
 */



  
function foo() {
	// возвращаем стрелочную функцию
	return (a) => {
		// Здесь `this` лексически заимствован из `foo()`
		console.log( this.a );
	};
}

var obj1 = {
	a: 2
};

var obj2 = {
	a: 3
};

var bar = foo.call( obj1 );
bar.call( obj2 ); // 2, а не 3!
/**
  Стрелочная функция, созданная в foo(), лексически захватывает любой this в foo() во время ее вызова. Поскольку в foo() this был привязан к obj1,
  bar (ссылка на возвращаемую стрелочную функцию) также будет с привязкой this к obj1. 
  Лексическая привязка стрелочной функции не может быть перекрыта (даже с помощью new!).
*/